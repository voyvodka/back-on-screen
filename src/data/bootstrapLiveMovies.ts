import { MovieRecord } from '../types/domain';

// Bootstrap data is no longer used at runtime.
// The addon now always waits for live API responses on cold start.
// This file is kept as a placeholder for potential future use.
export const bootstrapLiveMovies: MovieRecord[] = [];
